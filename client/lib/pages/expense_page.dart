import 'package:flutter/material.dart';

class Expensepage extends StatelessWidget {
  const Expensepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Expense Page")));
  }
}
